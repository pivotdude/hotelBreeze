'use client'
import { ConfirmBookingInput } from '@/modules/bookingHotel/queries/confirmBooking'

export default function ConfirmPaymentButton(props: ConfirmBookingInput) {
  // const onSubmit = (data: ConfirmBookingInput) => {
  //   const booking = confirmBooking(data)
  // }
  //
  // return (
  //   <Button onSubmit={() => onSubmit()} type="primary">
  //     Подтвердить бронирование
  //   </Button>
  // )
}
